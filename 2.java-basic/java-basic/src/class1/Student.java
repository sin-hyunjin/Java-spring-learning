package class1;

public class Student {
    /**
     *`class` 키워드를 사용해서 학생 클래스( `Student` )를 정의한다.
     * 학생 클래스는 내부에 이름( `name` ), 나이( `age` ), 성 적( `grade` ) 변수를 가진다.
     */
    String name;
    int age;
    int grade;
}
